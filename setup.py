from setuptools import setup

setup(
   name='cecksvc',
   version='1.0',
   description='A Web service to check connectivity with otheer wed services',
   author='Adam Young',
   author_email='ayoung@redhat.com',
   packages=['checksvc'],
   install_requires=[], 
)
